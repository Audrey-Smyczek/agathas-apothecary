extends StaticBody2D

#This is meant to allow for the caulderon to show the mixed colors inside using the blend() function when the potion and an ingredient is used

# Called when the node enters the scene tree for the first time.
func mixColorsInside():
	#has a transparent space inside the caulderon
	#when an ingredient is dropped into the caulderon (anywhere on it, not just empty space)
		#color will mix in
	pass 


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
